apply plugin: 'java'
apply plugin: 'application'

mainClassName = 'Weather'
version = '1.0'

repositories {
    mavenCentral()
}

dependencies {
    compile group: 'com.github.cliftonlabs', name: 'json-simple', version: '2.2.0'

}

run {
    main = 'Weather'
    standardInput = System.in
}